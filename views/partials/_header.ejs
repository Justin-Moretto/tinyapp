<header>
  <nav class="navbar navbar-expand-md navbar-dark bg-success">
    <a class="navbar-brand" href="/urls">TinyApp</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-item nav-link" href="/urls">My URLs</a>
        <a class="nav-item nav-link" href="/urls/new">Create New URL</a>
      </div>

      <!-- if no user signed in, display login form-->
      <% if (!currentUser){ %>
        <div style="float: right; padding-left: 66%;">
          <form action="/login" method="GET" style="float: left;">
            <button type="submit" class="btn btn-primary">Login</button>
          </form>
          <form action="/register" method="GET" style="float: left; margin-left: 5px;">
            <button type="submit" class="btn btn-primary">Register</button>
          </form>
        </div>
      <!-- otherwise, display username and logout button -->
      <% } else if (currentUser){ %>
          <div style="float: right; padding-left: 66%;">
            <form class="form-inline" action="/logout" method="POST" style="float: left">
              <h6> signed in as <b><%= userEmail %></b></h6>
              <button type="submit" class="btn btn-primary" style="margin-left: 5px;">Logout</button>
            </form>
          </div>
      <% } %>
    </div>
  </nav>
</header>